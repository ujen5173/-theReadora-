import BookSection from "~/app/_components/shared/books-section";

const PopularCompleted = () => {
  return <BookSection title="Completed Novel" scrollable={true} novels={[]} />;
};

export default PopularCompleted;
